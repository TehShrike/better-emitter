# 4.0.1

- only publish the js file and the type definition

# 4.0.0

- breaking: converted to ESM
- feature: the type definition now allows you to specify valid keys/arguments
- behind the scenes: tests are now TS, the types are now being tested

# 3.0.0

- breaking: only one optional event argument is supported now (used to allow any number of arguments)
- dropped runtime type assertions
- added `stopPropagation` functionality
